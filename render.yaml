services:
  - type: web
    name: trust-aesthetic-portfolio
    env: node
    rootDir: apps/server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: CORS_ORIGIN
        value: "*"
